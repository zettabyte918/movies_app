export class Movie {
  title: string;
  description: string;
  year: number;

  constructor(title: string, description: string, year: number) {
    this.title = title;
    this.description = description;
    this.year = year;
  }
}
